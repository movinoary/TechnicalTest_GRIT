const d = new Date();
const date = d.getDate();
const month = d.getMonth();
const year = d.getFullYear();
const fristName = "vino";
const gender = "pria";
const ifabula = "ifabula";

let input = `${date}${month}${year}${fristName}${gender}${ifabula}`;

console.log(input);
